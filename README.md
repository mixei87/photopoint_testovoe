# Система уведомлений

## 📋 Техническое задание

### 🎯 Задача

Создать сервис для отправки уведомлений пользователям через различные каналы связи.

### 📌 Требования

#### Основной функционал

- Отправка уведомлений пользователям через несколько каналов:
    - 📧 Email
    - 📱 SMS
    - ✉️ Telegram

#### Надежность доставки

- Реализовать механизм отказоустойчивости: если один способ доставки не сработал,
  система автоматически пробует следующий в очереди
- Ведение логов отправки уведомлений
- Возможность отслеживания статуса доставки

## 🚀 Технологии

- **Бэкенд**: Django + Django REST Framework
- **База данных**: PostgreSQL
- **Разработка**: Docker + docker-compose

## 📦 Установка и запуск

```bash
# Клонирование репозитория
git clone <repository-url>
cd photopoint_testovoe

# Создание и активация виртуального окружения
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env по необходимости

# Применение миграций
python manage.py migrate

# Запуск сервера разработки
python manage.py runserver
```

## 📄 API Endpoints

### Основные эндпоинты

- `GET /` - Главная страница
- `GET /health` - Проверка работоспособности сервиса
- `GET /status` - Статус сервиса
- `POST /api/notifications/` - Отправка уведомления
- `GET /api/notifications/` - Список уведомлений
- `GET /api/notifications/{id}/` - Детали уведомления

## 📝 Примеры запросов

### Отправка уведомления

```http
POST /api/notifications/
Content-Type: application/json

{
    "user_id": 1,
    "message": "Важное уведомление",
    "channels": ["email", "telegram", "sms"]
}
```

## 📊 Мониторинг

- **Админ-панель**: `/admin`

## 📄 Лицензия

MIT